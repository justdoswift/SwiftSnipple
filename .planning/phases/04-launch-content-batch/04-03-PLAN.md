---
phase: 04-launch-content-batch
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - content/snippets/like-button-bounce/snippet.yaml
  - content/snippets/bookmark-long-press/snippet.yaml
  - content/snippets/loadable-view-states/snippet.yaml
  - content/snippets/component-generation-prompt-pack/snippet.yaml
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
must_haves:
  truths:
    - The launch batch covers interaction, state, and AI-collaboration scenarios instead of stopping at static layout snippets
    - `like-button-bounce` is the only new interaction snippet that spends demo budget in this batch
    - `component-generation-prompt-pack` has a prompt-first reuse path that is as explicit as the code-first reuse path in the SwiftUI snippets
  artifacts:
    - path: content/snippets/like-button-bounce/snippet.yaml
      provides: the interaction-heavy demo-backed launch snippet
    - path: content/snippets/loadable-view-states/snippet.yaml
      provides: the reusable state-surface snippet spanning loading, empty, error, and content
    - path: content/snippets/component-generation-prompt-pack/snippet.yaml
      provides: the AI collaboration anchor required for SEED-02 coverage
  key_links:
    - from: content/snippets/like-button-bounce/snippet.yaml
      to: content/published/snippets.json
      why: the launch feed needs at least one interaction-first entry near the top of the batch
    - from: content/snippets/component-generation-prompt-pack/snippet.yaml
      to: content/published/search-documents.json
      why: prompt-first assets must still be searchable by their titles and AI workflow tags
---

<objective>
Fill the interaction, utility, and AI-template gaps in the launch batch.

Purpose: Complete the four required launch scenario bands so the public feed demonstrates actual breadth, not only UI polish.
Output: Four new snippets covering button feedback, long-press feedback, multi-state loading/error/content handling, and prompt-first AI component generation.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-content-batch/04-CONTEXT.md
@idea.md
@content/snippets/review-only-meter/snippet.yaml
@content/snippets/basic-card-feed/snippet.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the interaction pair with one demo-backed anchor</name>
  <files>content/snippets/like-button-bounce/snippet.yaml, content/snippets/bookmark-long-press/snippet.yaml</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, idea.md, content/snippets/basic-card-feed/snippet.yaml, content/snippets/stacked-hero-card/snippet.yaml</read_first>
  <action>Create `content/snippets/like-button-bounce` and `content/snippets/bookmark-long-press` as the launch interaction pair. Make `like-button-bounce` demo-backed with `assets.cover` and `assets.demo`, tuned for tap feedback and obvious motion. Make `bookmark-long-press` cover-only and frame it as haptic / contextual save feedback rather than another generic button. Both snippets must ship with copyable SwiftUI code and prompt assets, use controlled tags oriented around `interaction`, `button`, `bookmark`, `animation`, and `swiftui`, and keep summaries focused on what a developer can reuse immediately.</action>
  <verify>./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/like-button-bounce && ./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/bookmark-long-press</verify>
  <acceptance_criteria>
    - `content/snippets/like-button-bounce/snippet.yaml` contains `id: like-button-bounce`
    - `content/snippets/like-button-bounce/snippet.yaml` contains `demo: Media/demo.mp4`
    - `content/snippets/bookmark-long-press/snippet.yaml` contains `id: bookmark-long-press`
    - `content/snippets/bookmark-long-press/snippet.yaml` contains `cover: Media/cover.png`
    - `content/snippets/bookmark-long-press/snippet.yaml` does not contain `demo: Media/demo.mp4`
  </acceptance_criteria>
  <done>The launch batch now includes an obvious interaction story with one strong demo-backed clip and one lighter cover-only companion.</done>
</task>

<task type="auto">
  <name>Task 2: Add the utility and AI-template pair</name>
  <files>content/snippets/loadable-view-states/snippet.yaml, content/snippets/component-generation-prompt-pack/snippet.yaml</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, idea.md, content/snippets/review-only-meter/snippet.yaml</read_first>
  <action>Create `content/snippets/loadable-view-states` as a cover-only SwiftUI utility snippet that includes loading, empty, error, and content surface states under one reusable implementation. Create `content/snippets/component-generation-prompt-pack` as a prompt-first launch snippet whose `Code/Vibe` assets contain copyable prompts and acceptance checklists for generating reusable SwiftUI components. Both snippets must keep a single clear reuse path: `loadable-view-states` should emphasize the SwiftUI code path, while `component-generation-prompt-pack` should emphasize prompt assets and acceptance criteria, not video or ornamental media.</action>
  <verify>./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/loadable-view-states && ./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/component-generation-prompt-pack</verify>
  <acceptance_criteria>
    - `content/snippets/loadable-view-states/snippet.yaml` contains `id: loadable-view-states`
    - `content/snippets/loadable-view-states/snippet.yaml` contains `cover: Media/cover.png`
    - `content/snippets/component-generation-prompt-pack/snippet.yaml` contains `id: component-generation-prompt-pack`
    - `content/snippets/component-generation-prompt-pack/snippet.yaml` contains `prompt_root: Code/Vibe`
    - `content/snippets/component-generation-prompt-pack/snippet.yaml` does not contain `demo: Media/demo.mp4`
  </acceptance_criteria>
  <done>The launch batch now covers data-state and AI-collaboration use cases instead of stopping at pure UI composition.</done>
</task>

</tasks>

<verification>
- [ ] All four interaction/utility snippets pass schema validation
- [ ] `like-button-bounce` is the only new demo-backed interaction snippet
- [ ] `component-generation-prompt-pack` points to `Code/Vibe` for its main reuse path
- [ ] The launch batch now includes interaction, data state, and AI collaboration coverage required by SEED-02
</verification>

<success_criteria>
The launch batch is no longer lopsided toward layouts; it now demonstrates the four scenario bands that make SwiftSnippet feel broadly useful.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-content-batch/04-03-SUMMARY.md`
</output>
