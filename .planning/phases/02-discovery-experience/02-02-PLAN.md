---
phase: 02-discovery-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/internal/config/config.go
  - apps/api/internal/cache/memory.go
  - apps/api/internal/discovery/types.go
  - apps/api/internal/discovery/repository.go
  - apps/api/internal/discovery/service.go
  - apps/api/internal/discovery/service_test.go
  - apps/api/internal/httpapi/server.go
  - apps/api/internal/httpapi/server_test.go
autonomous: true
requirements:
  - DISC-02
  - DISC-03
  - DISC-04
  - OPS-03
must_haves:
  truths:
    - Feed, search, and detail APIs return only published records from the shared snapshot
    - Search supports keyword plus controlled facets and returns ranked results with fallback recommendations on zero results
    - Detail lookups distinguish `not_public` from missing slugs while caching only published feed/detail/search responses
  artifacts:
    - path: apps/api/internal/discovery/service.go
      provides: published-only query and ranking service
    - path: apps/api/internal/httpapi/server.go
      provides: public discovery HTTP endpoints
    - path: apps/api/internal/cache/memory.go
      provides: cache boundary for published read responses
  key_links:
    - from: apps/api/internal/discovery/repository.go
      to: content/published/snippets.json
      why: API responses must be hydrated from the Phase 2 snapshot instead of raw manifests or database tables
    - from: apps/api/internal/httpapi/server.go
      to: apps/api/internal/discovery/service.go
      why: all public HTTP handlers must share one visibility and caching boundary
---

<objective>
Implement the cached Go discovery APIs on top of the published snapshot.

Purpose: Make Phase 2 search, filter, feed, and detail behavior real at the backend boundary before the web routes are wired.
Output: Discovery repository/service types, in-memory published-response cache, and HTTP endpoints for feed, search, and detail.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-discovery-experience/02-CONTEXT.md
@.planning/phases/02-discovery-experience/02-RESEARCH.md
@.planning/phases/02-discovery-experience/02-01-PLAN.md
@apps/api/internal/httpapi/server.go
@apps/api/internal/httpapi/server_test.go
@apps/api/internal/config/config.go
@content/published/snippets.json
@content/published/visibility.json

<interfaces>
This plan consumes the contracts created in `02-01`:
```ts
type PublishedSnippetRecord = {
  id: string;
  title: string;
  summary: string;
  categoryPrimary: string;
  difficulty: 'easy' | 'medium' | 'hard';
  platforms: Array<{ os: string; minVersion: string }>;
  tags: string[];
  media: { coverUrl: string; demoUrl?: string };
  hasDemo: boolean;
  hasPrompt: boolean;
  featuredRank: number;
  publishedAt: string;
  codeBlocks: Array<{ id: string; filename: string; language: 'swift'; content: string }>;
  promptBlocks: Array<{ id: string; kind: 'prompt' | 'acceptance'; content: string }>;
  license: { code: string; media: string; thirdPartyNotice: string };
  dependencies: string[];
};
type SnippetVisibilityRecord = { id: string; visibility: 'published' | 'not_public' };
```

Existing server boundary:
```go
func NewServer(cfg config.Config) *http.Server
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define discovery repository and response contracts for the published snapshot</name>
  <files>apps/api/internal/config/config.go, apps/api/internal/discovery/types.go, apps/api/internal/discovery/repository.go</files>
  <read_first>.planning/phases/02-discovery-experience/02-CONTEXT.md, .planning/phases/02-discovery-experience/02-RESEARCH.md, .planning/phases/02-discovery-experience/02-01-PLAN.md, apps/api/internal/config/config.go, apps/api/internal/httpapi/server.go, content/published/snippets.json, content/published/visibility.json</read_first>
  <action>Create `apps/api/internal/discovery/types.go` with concrete API-facing types `FeedResponse`, `SearchResponse`, `FacetCounts`, `DetailResponse`, `NotPublicError`, and `SearchQuery`. `SearchQuery` must support exact fields `Q`, `Category`, `Difficulty`, `Platform`, `HasDemo`, and `HasPrompt`. Create `apps/api/internal/discovery/repository.go` with a repository that loads `content/published/snippets.json` and `content/published/visibility.json` from config paths, normalizes them into memory once per process, and exposes methods `LoadFeed(ctx)`, `Search(ctx, query)`, `LoadDetail(ctx, id)`, and `CheckVisibility(ctx, id)`. Update `apps/api/internal/config/config.go` to add exact config keys `PublishedIndexPath`, `VisibilityIndexPath`, and `DiscoveryCacheTTL`, defaulting to `content/published/snippets.json`, `content/published/visibility.json`, and `30s`.</action>
  <verify>grep -n 'PublishedIndexPath' apps/api/internal/config/config.go && grep -n 'type SearchQuery struct' apps/api/internal/discovery/types.go && grep -n 'LoadDetail' apps/api/internal/discovery/repository.go</verify>
  <acceptance_criteria>
    - `apps/api/internal/config/config.go` contains `PublishedIndexPath`
    - `apps/api/internal/config/config.go` contains `DiscoveryCacheTTL`
    - `apps/api/internal/discovery/types.go` contains `HasDemo` and `HasPrompt`
    - `apps/api/internal/discovery/repository.go` contains `content/published/snippets.json`
    - `apps/api/internal/discovery/repository.go` contains `CheckVisibility`
  </acceptance_criteria>
  <done>The API layer has explicit contracts and config for the Phase 2 published snapshot, with no dependency on draft-aware raw content records.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cached feed, search, and detail handlers with unpublished-state behavior</name>
  <files>apps/api/internal/cache/memory.go, apps/api/internal/discovery/service.go, apps/api/internal/discovery/service_test.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-discovery-experience/02-CONTEXT.md, apps/api/internal/discovery/types.go, apps/api/internal/discovery/repository.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go</read_first>
  <action>Create a simple in-memory TTL cache in `apps/api/internal/cache/memory.go` keyed only by published response identity. In `apps/api/internal/discovery/service.go`, implement feed sorting as `featuredRank ASC, publishedAt DESC` per D-04; search scoring as `title match * 3 + tags match * 2 + summary match * 1`, then sort by score desc, `featuredRank` asc, `publishedAt` desc; facet filters on exact query params `category`, `difficulty`, `platform`, `hasDemo`, and `hasPrompt`; and zero-result fallback as the first up to 3 published feed cards, preserving correctness when the snapshot only contains 2 published records. In `apps/api/internal/httpapi/server.go`, replace the Phase 1 placeholder with three concrete endpoints: `GET /api/v1/discovery/feed`, `GET /api/v1/discovery/search`, and `GET /api/v1/discovery/snippets/{id}`. The detail endpoint must return `403` with JSON body `{ "code": "not_public", "message": "内容未公开" }` when `CheckVisibility` says `not_public`, return `404` with `{ "code": "not_found" }` for unknown ids, and return `200` with `DetailResponse` for published ids. Add tests in `apps/api/internal/discovery/service_test.go` and `apps/api/internal/httpapi/server_test.go` that cover: feed omits `review-only-meter`; search by `q=card` returns ranked hits; `hasDemo=false` filter returns the no-demo record; zero-result search returns a `fallback` array whose length is `<= 3`; detail for `review-only-meter` returns 403; and detail for an unknown id returns 404.</action>
  <verify>cd apps/api && go test ./...</verify>
  <acceptance_criteria>
    - `apps/api/internal/httpapi/server.go` contains `"/api/v1/discovery/feed"`
    - `apps/api/internal/httpapi/server.go` contains `"/api/v1/discovery/search"`
    - `apps/api/internal/httpapi/server.go` contains `"/api/v1/discovery/snippets/"`
    - `apps/api/internal/httpapi/server.go` contains `"code": "not_public"` or `code\":\"not_public`
    - `apps/api/internal/discovery/service.go` contains `featuredRank`
    - `apps/api/internal/discovery/service.go` contains `<= 3` or `min(3`
    - `apps/api/internal/discovery/service.go` contains `fallback`
  </acceptance_criteria>
  <done>The Go API serves real published feed/search/detail responses with controlled filtering, ranking, explicit not-public handling, and published-only caching.</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/api && go test ./...` passes
- [ ] Feed/search never return `review-only-meter`
- [ ] Detail returns `403 not_public` for `review-only-meter`
- [ ] Cache is applied only after published-state filtering
</verification>

<success_criteria>
The API exposes stable public discovery endpoints with correct visibility behavior, search/filter semantics, and cache boundaries that future publish jobs can invalidate safely.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-experience/02-02-SUMMARY.md`
</output>
