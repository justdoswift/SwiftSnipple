---
phase: 03-publish-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
files_modified:
  - apps/api/internal/httpapi/server.go
  - apps/api/internal/httpapi/server_test.go
  - apps/api/README.md
  - README.md
  - package.json
  - scripts/db-migrate.sh
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05
  - OPS-01
must_haves:
  truths:
    - The API exposes an end-to-end publish flow that goes from upload-url request to review/publish action with actionable error responses
    - A successful publish refreshes public artifacts and leaves Discovery on a coherent public snapshot
    - Operator-facing docs and commands describe how to exercise and verify the publish pipeline locally
  artifacts:
    - path: apps/api/internal/httpapi/server.go
      provides: integrated Phase 3 write-path endpoints and publish orchestration hooks
    - path: apps/api/internal/httpapi/server_test.go
      provides: route-level end-to-end coverage for upload, review, publish, validation failures, and rate limiting
    - path: apps/api/README.md
      provides: local operator guide for the Phase 3 publish workflow
  key_links:
    - from: apps/api/internal/httpapi/server.go
      to: apps/api/internal/publish/artifacts.go
      why: the publish endpoint must trigger artifact refresh as part of the public release action
    - from: apps/api/internal/httpapi/server.go
      to: apps/api/internal/publish/validator.go
      why: publish requests must surface readiness failures before mutating public artifacts
    - from: apps/api/README.md
      to: apps/api/internal/httpapi/server.go
      why: local runbooks must match the exact write-path routes and payloads implemented
---

<objective>
Wire the Phase 3 publish pipeline into a verifiable API flow and document the local operator path.

Purpose: Turn the Phase 3 building blocks into a single testable workflow that maintainers can run locally and that downstream execution can validate without guesswork.
Output: Integrated publish HTTP handlers, end-to-end route coverage, and operator docs/scripts for local verification.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-publish-pipeline/03-CONTEXT.md
@.planning/phases/03-publish-pipeline/03-RESEARCH.md
@.planning/phases/03-publish-pipeline/03-01-PLAN.md
@.planning/phases/03-publish-pipeline/03-02-PLAN.md
@apps/api/internal/httpapi/server.go
@apps/api/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate the publish workflow into the HTTP boundary with end-to-end route coverage</name>
  <files>apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go</files>
  <read_first>.planning/phases/03-publish-pipeline/03-CONTEXT.md, .planning/phases/03-publish-pipeline/03-RESEARCH.md, .planning/phases/03-publish-pipeline/03-01-PLAN.md, .planning/phases/03-publish-pipeline/03-02-PLAN.md, apps/api/internal/httpapi/server.go</read_first>
  <action>Update `apps/api/internal/httpapi/server.go` so the signed-upload endpoint, review transition endpoint, and publish endpoint are all wired to the Phase 3 services created in the prior plans. The publish endpoint must run readiness validation, perform the explicit publish transition, refresh public artifacts, and return a structured success response that includes which artifact outputs were updated. Validation failures must return structured error payloads rather than generic `internal_error`; rate-limit responses must remain distinguishable from validation or transition failures. Expand `apps/api/internal/httpapi/server_test.go` with end-to-end tests that cover: successful upload-url issuance, successful review transition, publish rejection due to readiness issues, successful publish that updates public artifacts, and rate-limit rejection on repeated write requests.</action>
  <verify>cd apps/api && go test ./internal/httpapi</verify>
  <acceptance_criteria>
    - `apps/api/internal/httpapi/server.go` returns a non-500 structured response for publish readiness failures
    - `apps/api/internal/httpapi/server.go` returns artifact update metadata on successful publish
    - `apps/api/internal/httpapi/server_test.go` contains a success test for publish
    - `apps/api/internal/httpapi/server_test.go` contains a failure test for publish readiness or rate limiting
  </acceptance_criteria>
  <done>The API now exposes a coherent end-to-end Phase 3 workflow instead of disconnected backend primitives.</done>
</task>

<task type="auto">
  <name>Task 2: Document and script the local Phase 3 operator workflow</name>
  <files>apps/api/README.md, README.md, package.json, scripts/db-migrate.sh</files>
  <read_first>.planning/ROADMAP.md, .planning/phases/03-publish-pipeline/03-CONTEXT.md, apps/api/README.md, README.md, package.json</read_first>
  <action>Update `apps/api/README.md` and the root `README.md` so maintainers can locally exercise the Phase 3 workflow with copy-pastable commands: starting the DB, running migrations, starting the API, requesting an upload URL, moving a snippet to review, and publishing a version that refreshes public artifacts. If the repository needs one or two small command aliases in `package.json` to make this reproducible, add them explicitly and document them. Keep `scripts/db-migrate.sh` aligned with the publish workflow docs if any migration or environment assumptions changed during prior plans. The docs must clearly state which endpoints are Phase 3 write-path endpoints and what artifacts should be updated after a successful publish.</action>
  <verify>grep -n \"upload-url\\|review\\|publish\" apps/api/README.md README.md && grep -n \"content/published\" apps/api/README.md README.md</verify>
  <acceptance_criteria>
    - `apps/api/README.md` contains `POST /api/v1/media/upload-url`
    - `apps/api/README.md` contains `review` and `publish`
    - `README.md` contains at least one Phase 3 local verification command
    - `README.md` or `apps/api/README.md` contains `content/published`
  </acceptance_criteria>
  <done>Phase 3 has a reproducible operator path, so execution and verification can test the publish workflow without reverse-engineering the code.</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/api && go test ./internal/httpapi` passes
- [ ] README and API docs describe the local publish workflow and expected artifact outputs
- [ ] Successful publish responses identify artifact refresh outputs
- [ ] Validation and rate-limit failures are distinguishable at the HTTP layer
</verification>

<success_criteria>
Maintainers can run and understand the complete Phase 3 publish flow locally, from upload URL issuance to artifact refresh, with tests and docs that make the path explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/03-publish-pipeline/03-03-SUMMARY.md`
</output>
