---
phase: 01-foundation-protocol
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - .github/workflows/ci.yml
  - .swiftlint.yml
  - scripts/check-swift.sh
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - CPRT-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - CI blocks on protocol validation, Web checks, API build/test, and Swift quality gate entrypoints
    - Documentation explains how to run protocol validation, local infrastructure, and Phase 2/3 handoff assumptions
    - Swift checks fail clearly when required tooling is missing instead of silently passing
  artifacts:
    - path: .github/workflows/ci.yml
      provides: reproducible core gate workflow
    - path: scripts/check-swift.sh
      provides: explicit Swift quality gate entrypoint
    - path: README.md
      provides: developer setup and contract handoff documentation
  key_links:
    - from: package.json
      to: .github/workflows/ci.yml
      why: CI must call the same root commands developers run locally
    - from: scripts/check-swift.sh
      to: .swiftlint.yml
      why: Swift lint behavior must be configured and reproducible
---

<objective>
Wire the Phase 1 quality gates and documentation handoff on top of the repository skeleton and protocol package.

Purpose: Ensure the new baseline is enforceable in CI and understandable to future phases.
Output: Core CI workflow, Swift quality gate script/config, updated project documentation, and explicit handoff constraints.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-protocol/01-CONTEXT.md
@.planning/phases/01-foundation-protocol/01-RESEARCH.md
@README.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the core CI and Swift quality gate entrypoints</name>
  <files>.github/workflows/ci.yml, .swiftlint.yml, scripts/check-swift.sh</files>
  <read_first>.planning/phases/01-foundation-protocol/01-CONTEXT.md, .planning/phases/01-foundation-protocol/01-RESEARCH.md, package.json, scripts/check-swift.sh</read_first>
  <action>Create a GitHub Actions workflow named `CI` that runs on pushes to `main` and on pull requests, provisions PostgreSQL 16 as a service, installs pnpm and Go, installs SwiftLint and a `swift-format` binary, runs `pnpm install --frozen-lockfile`, runs `pnpm db:migrate`, and then executes `pnpm check`. Configure `.swiftlint.yml` to point at the valid Swift fixture path. In `scripts/check-swift.sh`, support both `swift-format` and `swift format` as formatter entrypoints, require `swiftlint`, lint the fixture directory recursively, and run `swift build --package-path "$FIXTURE_SWIFT_DIR"` to validate the sample package.</action>
  <verify>grep -n "pnpm check" .github/workflows/ci.yml && grep -n "postgres:16" .github/workflows/ci.yml && grep -n 'swift build --package-path "$FIXTURE_SWIFT_DIR"' scripts/check-swift.sh</verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `name: CI`
    - `.github/workflows/ci.yml` contains `postgres:16`
    - `.github/workflows/ci.yml` contains `pnpm db:migrate`
    - `scripts/check-swift.sh` contains `swift format --help` fallback logic or equivalent dual-entry handling
    - `.swiftlint.yml` contains `packages/snippet-schema/fixtures/valid/basic-card-feed/Code/SwiftUI`
  </acceptance_criteria>
  <done>The repository has a reproducible CI definition and explicit Swift quality gate commands.</done>
</task>

<task type="auto">
  <name>Task 2: Document local workflows and downstream contract boundaries</name>
  <files>README.md, AGENTS.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-foundation-protocol/01-CONTEXT.md, README.md, AGENTS.md</read_first>
  <action>Update the project documentation so contributors can install dependencies, start PostgreSQL, apply migrations, run the Web and API stubs, validate snippet fixtures, and understand the exact Phase 2/3 handoff boundaries. The README must explicitly document that `packages/snippet-schema` is the single source of truth for `snippet.yaml`, that Phase 2 must reuse the published-content contract, and that Phase 3 must reuse the publish state and asset contract. Update `AGENTS.md` so its “Next Command” guidance no longer points to planning as if it were still pending; instead, reflect that execution/verification should now follow the generated plans or current implemented Phase 1 baseline.</action>
  <verify>grep -n "Phase 2 must reuse" README.md && grep -n "Phase 3 must reuse" README.md && grep -n "single source of truth" README.md && grep -n "Next Command" AGENTS.md</verify>
  <acceptance_criteria>
    - `README.md` contains `packages/snippet-schema` and `single source of truth`
    - `README.md` contains `Phase 2` and `Phase 3` handoff constraints
    - `README.md` contains `pnpm check`
    - `AGENTS.md` no longer tells the user to run `$gsd-plan-phase 1`
  </acceptance_criteria>
  <done>Developers and future agents can follow the documented setup and contract boundaries without rediscovering Phase 1 intent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm check` passes, or if it fails, the failure is only due to missing local `swiftlint`
- [ ] CI workflow mirrors the documented local commands
- [ ] README captures Phase 2/3 handoff expectations explicitly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- CI and docs enforce the same Phase 1 contract established by the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-protocol/01-03-SUMMARY.md`
</output>
