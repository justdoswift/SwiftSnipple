---
phase: 04-launch-content-batch
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
  - 04-03-PLAN.md
files_modified:
  - apps/api/internal/publish/artifacts.go
  - apps/api/internal/publish/artifacts_test.go
  - apps/api/internal/discovery/service_test.go
  - apps/api/internal/httpapi/server_test.go
  - content/published/snippets.json
  - content/published/visibility.json
  - content/published/search-documents.json
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
must_haves:
  truths:
    - All 12 launch snippets pass through the real `draft -> review -> published` flow and refresh the public artifacts consumed by Discovery
    - The public feed reaches 12 published items with exactly 4 demo-backed entries and a deliberate featured order favoring showcase content
    - Discovery regression coverage still proves unpublished content stays hidden while the new launch batch appears in feed, search, and detail
  artifacts:
    - path: apps/api/internal/publish/artifacts.go
      provides: deterministic launch ordering and artifact generation for the published feed, visibility registry, and search documents
    - path: content/published/snippets.json
      provides: the 12-item public launch feed snapshot
    - path: content/published/visibility.json
      provides: the published/not_public visibility registry used by detail blocking logic
  key_links:
    - from: apps/api/internal/publish/artifacts.go
      to: content/published/snippets.json
      why: featured-rank ordering must be encoded at artifact generation time, not hand-edited in checked-in JSON
    - from: apps/api/internal/discovery/service_test.go
      to: content/published/search-documents.json
      why: discovery tests must reflect the exact launch batch size, demo counts, and search surface exported by the publish artifacts
    - from: content/published/visibility.json
      to: apps/api/internal/httpapi/server_test.go
      why: the launch batch cannot regress explicit `not_public` behavior while expanding the public feed
---

<objective>
Publish the full launch batch and verify the public discovery experience against real launch content.

Purpose: Finish Phase 4 by turning the content batch into actual published outputs and proving the whole discovery story works with 12 launch-ready snippets.
Output: Refreshed published artifacts, launch-order tests, discovery regressions updated for the 12-item batch, and a verified demo path from discovery to reuse.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-content-batch/04-CONTEXT.md
@.planning/phases/03-publish-pipeline/03-UAT.md
@apps/api/README.md
@apps/api/internal/publish/artifacts.go
@content/published/snippets.json
@content/published/visibility.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish all 12 launch snippets through the real artifact pipeline</name>
  <files>apps/api/internal/publish/artifacts.go, apps/api/internal/publish/artifacts_test.go, content/published/snippets.json, content/published/visibility.json, content/published/search-documents.json</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, .planning/phases/03-publish-pipeline/03-UAT.md, apps/api/README.md, apps/api/internal/publish/artifacts.go, content/published/snippets.json, content/published/visibility.json</read_first>
  <action>Use the existing Phase 3 publish endpoints and compiler to move all 12 launch snippets into `published` state and regenerate the checked-in public artifacts. Keep the published set exactly to these ids: `basic-card-feed`, `stacked-hero-card`, `review-only-meter`, `universal-card-container`, `masonry-grid`, `skeleton-shimmer`, `tag-chips-wrap`, `like-button-bounce`, `bookmark-long-press`, `search-page-skeleton`, `loadable-view-states`, `component-generation-prompt-pack`. Preserve one explicit `not_public` regression sample in `content/published/visibility.json` as `internal-draft-sample`. Add or update deterministic launch ordering in `apps/api/internal/publish/artifacts.go` so featured ranks sort as: `stacked-hero-card` 1, `masonry-grid` 2, `basic-card-feed` 3, `skeleton-shimmer` 4, `like-button-bounce` 5, `universal-card-container` 6, `tag-chips-wrap` 7, `bookmark-long-press` 8, `search-page-skeleton` 9, `loadable-view-states` 10, `review-only-meter` 11, `component-generation-prompt-pack` 12. Add an artifact test that asserts this exact launch order.</action>
  <verify>cd apps/api && go test ./internal/publish && pnpm check:published-index</verify>
  <acceptance_criteria>
    - `apps/api/internal/publish/artifacts.go` contains `launchFeaturedRanks`
    - `apps/api/internal/publish/artifacts.go` contains `\"stacked-hero-card\":                  1`
    - `apps/api/internal/publish/artifacts_test.go` contains `TestFeaturedRankForSnippetLaunchBatchOrder`
    - `content/published/snippets.json` contains `"id": "component-generation-prompt-pack"`
    - `content/published/visibility.json` contains `"id": "internal-draft-sample"`
    - `content/published/search-documents.json` contains `"items"`
  </acceptance_criteria>
  <done>The launch batch is now real published content with deterministic ordering and checked-in public artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Update discovery and HTTP regressions to the 12-item launch batch and prove the demo path</name>
  <files>apps/api/internal/discovery/service_test.go, apps/api/internal/httpapi/server_test.go</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, apps/api/internal/discovery/service_test.go, apps/api/internal/httpapi/server_test.go, content/published/snippets.json, content/published/search-documents.json</read_first>
  <action>Update `apps/api/internal/discovery/service_test.go` and `apps/api/internal/httpapi/server_test.go` so they assert the launch-state discovery contract rather than the old two-item seed fixture. Expect `12` published feed items, `4` demo-backed items, and a ranked `button` search result led by `like-button-bounce`. Keep the hidden-content regression by asserting `internal-draft-sample` is blocked from public detail/search. Then run the full launch verification stack: `pnpm lint:protocol`, snippet-schema validation for all 12 snippet directories, `cd apps/api && go test ./internal/discovery ./internal/httpapi ./internal/publish`, and `pnpm check:published-index`. Use those results to validate the end-to-end launch story: discovery feed reaches 12 items, search hits the new titles/tags, detail hides absent tabs, and unpublished content stays hidden.</action>
  <verify>pnpm lint:protocol && cd apps/api && go test ./internal/discovery ./internal/httpapi ./internal/publish && pnpm check:published-index</verify>
  <acceptance_criteria>
    - `apps/api/internal/discovery/service_test.go` contains `12`
    - `apps/api/internal/discovery/service_test.go` contains `like-button-bounce`
    - `apps/api/internal/discovery/service_test.go` contains `internal-draft-sample`
    - `apps/api/internal/httpapi/server_test.go` contains `like-button-bounce`
    - `apps/api/internal/httpapi/server_test.go` contains `internal-draft-sample`
  </acceptance_criteria>
  <done>The launch batch is covered by regression tests that match the public site the user will actually browse.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm lint:protocol` passes
- [ ] All 12 snippet directories pass the snippet schema CLI
- [ ] `cd apps/api && go test ./internal/discovery ./internal/httpapi ./internal/publish` passes
- [ ] `pnpm check:published-index` passes
- [ ] `content/published/snippets.json` contains 12 published launch items and exactly 4 demo-backed items
- [ ] Search, detail, and `not_public` regressions are updated for the launch batch
</verification>

<success_criteria>
The Phase 4 deliverable is no longer "content exists in the repo"; it is a real 12-item launch batch that the public discovery product can browse, search, and reuse immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-content-batch/04-04-SUMMARY.md`
</output>
