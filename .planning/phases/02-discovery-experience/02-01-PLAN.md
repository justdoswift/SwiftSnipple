---
phase: 02-discovery-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/snippet-schema/package.json
  - packages/snippet-schema/src/published-index.ts
  - packages/snippet-schema/src/check-published-index.ts
  - packages/snippet-schema/src/run-tests.ts
  - content/published/snippets.json
  - content/published/visibility.json
autonomous: true
requirements:
  - DISC-01
  - DISC-04
  - DTL-01
  - DTL-02
  - DTL-03
  - DTL-04
  - OPS-03
must_haves:
  truths:
    - Public discovery reads from a published-only snapshot instead of raw mutable snippet records
    - The snapshot contains enough card, detail, code, prompt, and license data for the web experience without exposing draft metadata
    - Direct slug checks can distinguish a known unpublished snippet from a missing slug without making unpublished items enumerable
  artifacts:
    - path: packages/snippet-schema/src/published-index.ts
      provides: published discovery contracts and validation helpers
    - path: content/published/snippets.json
      provides: published-only feed/search/detail snapshot
    - path: content/published/visibility.json
      provides: slug-to-visibility registry for explicit not-public handling
  key_links:
    - from: packages/snippet-schema/src/check-published-index.ts
      to: content/published/snippets.json
      why: the published snapshot must be machine-validated before API or web code consumes it
    - from: content/published/visibility.json
      to: content/published/snippets.json
      why: published items must also exist in the visibility registry while draft/review items remain absent from the published snapshot
---

<objective>
Define the published-only discovery read model and seed it with realistic public sample data.

Purpose: Give Phase 2 a single public content contract so feed, search, detail, caching, and unpublished-state handling all build on the same source.
Output: TypeScript contracts and validation for the published index, plus a curated `content/published` snapshot and visibility registry.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-discovery-experience/02-CONTEXT.md
@.planning/phases/02-discovery-experience/02-RESEARCH.md
@packages/snippet-schema/src/types.ts
@packages/snippet-schema/src/schema.ts
@packages/snippet-schema/fixtures/valid/basic-card-feed/snippet.yaml

<interfaces>
From `packages/snippet-schema/src/types.ts`:
```ts
export type SnippetManifest = {
  id: string;
  title: string;
  summary: string;
  status: 'draft' | 'review' | 'published';
  category_primary: CategoryPrimary;
  difficulty: Difficulty;
  platforms: Array<{ os: 'iOS' | 'macOS' | 'watchOS' | 'visionOS'; min_version: string }>;
  assets: { cover: string; demo?: string };
  code: { swiftui_root: string; prompt_root: string; tests_root: string };
  license: { code: string; media: string; third_party_notice: string };
  source_revision: string;
};
```

Phase 2 published snapshot must stay inside those manifest concepts and add only public-view fields such as ranking, copy blocks, and disclosure blocks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the published discovery snapshot contract and validator</name>
  <files>package.json, packages/snippet-schema/package.json, packages/snippet-schema/src/published-index.ts, packages/snippet-schema/src/check-published-index.ts, packages/snippet-schema/src/run-tests.ts</files>
  <read_first>.planning/phases/02-discovery-experience/02-CONTEXT.md, .planning/phases/02-discovery-experience/02-RESEARCH.md, packages/snippet-schema/src/types.ts, packages/snippet-schema/package.json, packages/snippet-schema/src/run-tests.ts</read_first>
  <action>Create `packages/snippet-schema/src/published-index.ts` exporting the exact Phase 2 public types: `PublishedSnippetCard`, `PublishedSnippetDetail`, `PublishedSnippetSearchDocument`, `PublishedSnippetRecord`, `PublishedIndexEnvelope`, `SnippetVisibilityRecord`, and `VisibilityIndexEnvelope`. The card/detail model must include `id`, `title`, `summary`, `categoryPrimary`, `difficulty`, `platforms`, `tags`, `media.coverUrl`, optional `media.demoUrl`, booleans `hasDemo` and `hasPrompt`, `featuredRank`, `publishedAt`, `codeBlocks`, `promptBlocks`, `license`, and `dependencies`. Also export validation helpers `parsePublishedIndex(json: string)` and `parseVisibilityIndex(json: string)` that throw on missing keys, duplicate ids, published items without `visibility: "published"`, or any record whose `status` is present in the public snapshot. Add `packages/snippet-schema/src/check-published-index.ts` with a CLI mode that validates `content/published/snippets.json` and `content/published/visibility.json`. Update `packages/snippet-schema/package.json` and root `package.json` to add the exact commands `verify:published-index` and `check:published-index`. Extend `packages/snippet-schema/src/run-tests.ts` so it asserts the validator accepts the curated snapshot and rejects duplicate-id or missing-visibility fixtures created inline in the test file.</action>
  <verify>pnpm --filter @swiftsnippet/snippet-schema test && pnpm --filter @swiftsnippet/snippet-schema verify:published-index</verify>
  <acceptance_criteria>
    - `packages/snippet-schema/src/published-index.ts` contains `export type PublishedSnippetCard`
    - `packages/snippet-schema/src/published-index.ts` contains `featuredRank` and `publishedAt`
    - `packages/snippet-schema/src/published-index.ts` contains `parsePublishedIndex` and `parseVisibilityIndex`
    - `packages/snippet-schema/package.json` contains `"verify:published-index": "tsx src/check-published-index.ts"`
    - `package.json` contains `"check:published-index": "pnpm --filter @swiftsnippet/snippet-schema verify:published-index"`
  </acceptance_criteria>
  <done>The repo has a machine-checked public discovery contract that downstream API and web code can consume without re-inventing visibility rules.</done>
</task>

<task type="auto">
  <name>Task 2: Seed the published-only snapshot and explicit visibility registry</name>
  <files>content/published/snippets.json, content/published/visibility.json</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-discovery-experience/02-CONTEXT.md, packages/snippet-schema/fixtures/valid/basic-card-feed/snippet.yaml, content/published/snippets.json, content/published/visibility.json</read_first>
  <action>Create `content/published/snippets.json` with a top-level object containing `generatedAt` and `items`. Seed exactly two published records named `basic-card-feed` and `stacked-hero-card`, both with `media.coverUrl`, `featuredRank`, `publishedAt`, `platforms`, `codeBlocks`, `promptBlocks`, `license`, `dependencies`, and booleans for `hasDemo` / `hasPrompt`. Keep `basic-card-feed` as the easiest entry and give `stacked-hero-card` a lower `featuredRank` number than its recency so feed sorting can blend editorial preference with freshness per D-04. Create `content/published/visibility.json` with `generatedAt` and `items`, containing the two published ids plus one explicit non-public id `review-only-meter` whose record is `{ "id": "review-only-meter", "visibility": "not_public" }` and nothing else. Do not include any title, summary, tags, or other metadata for `review-only-meter`. Ensure `review-only-meter` is absent from `content/published/snippets.json` per D-14. Ensure `stacked-hero-card` omits `media.demoUrl` but still has a valid `coverUrl` and `hasDemo: false` per D-15. Ensure one published record omits prompt blocks and sets `hasPrompt: false` so the detail UI can hide the Prompt tab per D-16.</action>
  <verify>grep -n '"id": "basic-card-feed"' content/published/snippets.json && grep -n '"id": "stacked-hero-card"' content/published/snippets.json && grep -n '"id": "review-only-meter"' content/published/visibility.json && ! grep -n '"review-only-meter"' content/published/snippets.json && pnpm check:published-index</verify>
  <acceptance_criteria>
    - `content/published/snippets.json` contains `"items"`
    - `content/published/snippets.json` contains `"featuredRank"`
    - `content/published/snippets.json` contains `"hasDemo": false`
    - `content/published/snippets.json` contains `"hasPrompt": false`
    - `content/published/visibility.json` contains `"visibility": "not_public"`
  </acceptance_criteria>
  <done>Phase 2 has a realistic published snapshot and a minimal non-public slug registry that enforce public-only enumeration rules.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @swiftsnippet/snippet-schema test` passes
- [ ] `pnpm --filter @swiftsnippet/snippet-schema verify:published-index` passes
- [ ] `content/published/snippets.json` contains only published records
- [ ] `content/published/visibility.json` contains the non-public slug without leaking extra metadata
</verification>

<success_criteria>
The repository contains a validated public discovery snapshot, a separate visibility registry for explicit not-public lookups, and reusable contracts that later plans can import directly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-experience/02-01-SUMMARY.md`
</output>
