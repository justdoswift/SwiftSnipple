---
phase: 08-admin-studio-v1
plan: 01
type: execute
wave: 1
depends_on:
  - 03-publish-pipeline
files_modified:
  - apps/api/internal/config/config.go
  - apps/api/internal/httpapi/server.go
  - apps/api/internal/httpapi/admin_auth.go
  - apps/api/internal/publish/admin_store.go
  - apps/api/internal/publish/repository.go
  - apps/web/src/lib/studio/api.ts
  - apps/web/src/lib/studio/types.ts
  - apps/web/src/routes/studio/+layout.ts
  - apps/web/src/routes/studio/+layout.svelte
  - apps/web/src/routes/studio/login/+page.svelte
  - apps/web/src/routes/studio/login/+page.server.ts
  - apps/web/src/routes/studio/+page.svelte
  - apps/web/src/routes/studio/snippets/+page.svelte
  - apps/web/src/routes/studio/snippets/new/+page.svelte
  - apps/web/src/routes/studio/snippets/[id]/+page.svelte
  - apps/web/e2e/studio.spec.ts
  - apps/web/playwright.config.ts
  - scripts/dev.sh
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-03
  - OPS-04
must_haves:
  truths:
    - The admin studio must write back into the repository content protocol instead of creating a second CMS source of truth
    - Review and publish actions must reuse the existing publish pipeline boundaries
    - Studio authentication must be server-enforced for both pages and write APIs
  artifacts:
    - path: apps/api/internal/publish/admin_store.go
      provides: file-protocol read/write, skeleton generation, validation, and asset preview/upload
    - path: apps/api/internal/httpapi/server.go
      provides: protected admin session, snippet, upload, review, and publish routes
    - path: apps/web/src/routes/studio/snippets/[id]/+page.svelte
      provides: the end-to-end editing surface for metadata, code, prompt, media, and publish actions
  key_links:
    - from: apps/web/src/routes/studio/login/+page.server.ts
      to: apps/api/internal/httpapi/admin_auth.go
      why: the login surface and cookie session contract must align or the backend cannot be accessed reliably
    - from: apps/api/internal/publish/admin_store.go
      to: content/snippets
      why: studio editing only matters if changes land back in the repository protocol
---

> Historical note: this plan was authored before the current frontend stack settled. `SvelteKit` references here are retained as historical execution context only.

<objective>
Retroactively capture the first complete internal admin studio implementation.

Purpose: Turn the already-implemented backend and `/studio` work into a first-class GSD phase so the team can verify, iterate, and extend it without losing context.
Output: Protected admin APIs, SvelteKit studio routes, file-protocol editing, and automated coverage for the main operator path.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-admin-studio-v1/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship the v1 admin studio across API, filesystem write path, and SvelteKit UI</name>
  <files>apps/api/internal/config/config.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/admin_auth.go, apps/api/internal/publish/admin_store.go, apps/api/internal/publish/repository.go, apps/web/src/lib/studio/api.ts, apps/web/src/lib/studio/types.ts, apps/web/src/routes/studio/+layout.ts, apps/web/src/routes/studio/+layout.svelte, apps/web/src/routes/studio/login/+page.svelte, apps/web/src/routes/studio/login/+page.server.ts, apps/web/src/routes/studio/+page.svelte, apps/web/src/routes/studio/snippets/+page.svelte, apps/web/src/routes/studio/snippets/new/+page.svelte, apps/web/src/routes/studio/snippets/[id]/+page.svelte, apps/web/e2e/studio.spec.ts, apps/web/playwright.config.ts, scripts/dev.sh</files>
  <action>Protect admin write routes with a single-password cookie session, add a file-backed admin store that reads and writes `content/snippets/<id>` plus local media assets, expose admin list/detail/create/save/validate endpoints, and build the `/studio` SvelteKit surface for login, dashboard, snippet list, new snippet creation, editing, media upload, validation, review, and publish. Stabilize local dev and Playwright startup so e2e uses isolated ports and deterministic admin credentials.</action>
  <verify>cd apps/api && go test ./... && pnpm -C apps/web run check && pnpm -C apps/web run test && pnpm -C apps/web run test:e2e</verify>
  <acceptance_criteria>
    - `/studio/login` accepts the configured admin password and redirects to `/studio`
    - `POST /api/v1/admin/snippets` creates a new snippet skeleton that can be loaded in the editor
    - `PUT /api/v1/admin/snippets/{id}` writes metadata, code, prompt, and license content back to the snippet directory
    - `POST /api/v1/admin/snippets/{id}/validate` returns concrete issues when a snippet is not publish-ready
    - Review / publish actions still flow through the existing `/api/v1/publish/snippets/...` routes under admin auth
  </acceptance_criteria>
  <done>The operator path is no longer “edit files by hand”; the project now has a working internal admin studio rooted in the repository protocol.</done>
</task>

</tasks>

<verification>
- [x] `cd apps/api && go test ./...`
- [x] `pnpm -C apps/web run check`
- [x] `pnpm -C apps/web run test`
- [x] `pnpm -C apps/web run test:e2e`
</verification>

<success_criteria>
SwiftSnippet now has a working internal studio that can create, edit, validate, and publish snippets without leaving the product surface.
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-studio-v1/08-01-SUMMARY.md`
</output>
