---
phase: 03-publish-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - package.json
  - packages/snippet-schema/package.json
  - packages/snippet-schema/src/check-published-index.ts
  - packages/snippet-schema/src/published-index.ts
  - apps/api/internal/publish/validator.go
  - apps/api/internal/publish/validator_test.go
  - apps/api/internal/publish/artifacts.go
  - apps/api/internal/publish/artifacts_test.go
  - content/published/snippets.json
  - content/published/visibility.json
  - content/published/search-documents.json
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
  - PIPE-05
must_haves:
  truths:
    - Publish readiness reuses the existing snippet protocol validator and adds publish-only media/license/release checks
    - A publish action can regenerate the public snapshot and visibility registry that Phase 2 Discovery already consumes
    - Search payloads are refreshed as part of the same publish artifact pass so newly published content becomes discoverable
  artifacts:
    - path: apps/api/internal/publish/validator.go
      provides: layered publish-readiness checks that extend the existing content protocol gate
    - path: apps/api/internal/publish/artifacts.go
      provides: public artifact compiler for snapshot, visibility registry, and search documents
    - path: content/published/search-documents.json
      provides: explicit publish-time search payload for later indexing/query adapters
  key_links:
    - from: apps/api/internal/publish/validator.go
      to: packages/snippet-schema/src/validate.ts
      why: publish validation must call the existing protocol validator rather than reimplementing schema rules
    - from: apps/api/internal/publish/artifacts.go
      to: content/published/snippets.json
      why: the compiler must emit the exact public snapshot Discovery already reads
    - from: apps/api/internal/publish/artifacts.go
      to: content/published/search-documents.json
      why: search refresh must be a first-class publish output instead of a read-time side effect
---

<objective>
Add the publish-readiness gate and artifact compiler for Phase 3.

Purpose: Turn explicit publish actions into validated public outputs instead of direct state flips.
Output: A layered validator, deterministic artifact generation for snapshot/visibility/search payloads, and tests proving publish refresh updates the read-side artifacts Discovery depends on.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-publish-pipeline/03-CONTEXT.md
@.planning/phases/03-publish-pipeline/03-RESEARCH.md
@packages/snippet-schema/src/validate.ts
@packages/snippet-schema/src/published-index.ts
@content/published/snippets.json
@content/published/visibility.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement layered publish-readiness validation</name>
  <files>apps/api/internal/publish/validator.go, apps/api/internal/publish/validator_test.go, packages/snippet-schema/src/check-published-index.ts</files>
  <read_first>.planning/phases/03-publish-pipeline/03-CONTEXT.md, .planning/phases/03-publish-pipeline/03-RESEARCH.md, packages/snippet-schema/src/validate.ts, packages/snippet-schema/src/check-published-index.ts, infra/postgres/migrations/001_init.sql</read_first>
  <action>Create `apps/api/internal/publish/validator.go` with a layered validator that first delegates to the existing snippet protocol/asset validator and then applies publish-only checks for media readiness, license completeness, required asset presence for the chosen publish version, and release metadata completeness. Return a structured list of issues that includes machine-readable codes and human-readable messages so the publish endpoint can report actionable failures. Add `apps/api/internal/publish/validator_test.go` covering at least one happy path plus failures for missing cover assets, missing license notices, and version/asset mismatches. If needed, extend `packages/snippet-schema/src/check-published-index.ts` only to keep the publish-validator output aligned with the current artifact validation CLI, but do not fork the schema rules.</action>
  <verify>cd apps/api && go test ./internal/publish && pnpm --filter @swiftsnippet/snippet-schema verify:published-index</verify>
  <acceptance_criteria>
    - `apps/api/internal/publish/validator.go` contains an issue struct or type with `Code` and `Message`
    - `apps/api/internal/publish/validator.go` delegates to existing protocol validation rather than defining a second manifest parser
    - `apps/api/internal/publish/validator_test.go` contains a missing-cover or missing-license failure case
    - `apps/api/internal/publish/validator_test.go` contains at least one successful readiness case
  </acceptance_criteria>
  <done>Publish actions can fail for clear, inspectable reasons before any public artifact is refreshed.</done>
</task>

<task type="auto">
  <name>Task 2: Compile published snapshot, visibility registry, and search documents from publish state</name>
  <files>package.json, packages/snippet-schema/package.json, packages/snippet-schema/src/published-index.ts, apps/api/internal/publish/artifacts.go, apps/api/internal/publish/artifacts_test.go, content/published/snippets.json, content/published/visibility.json, content/published/search-documents.json</files>
  <read_first>.planning/phases/03-publish-pipeline/03-CONTEXT.md, .planning/phases/03-publish-pipeline/03-RESEARCH.md, packages/snippet-schema/src/published-index.ts, content/published/snippets.json, content/published/visibility.json</read_first>
  <action>Create `apps/api/internal/publish/artifacts.go` that can take published snippet/version/asset records and emit three deterministic outputs: `content/published/snippets.json`, `content/published/visibility.json`, and a new `content/published/search-documents.json` envelope. Keep the snapshot and visibility files compatible with the current `packages/snippet-schema/src/published-index.ts` parser, and extend that parser only if a backward-compatible search-document envelope type is needed for the new file. Update root/package scripts only if necessary to add an explicit artifact-refresh or artifact-check command that planners and executors can run. Add `apps/api/internal/publish/artifacts_test.go` to verify that publishing a new version updates the snapshot, keeps `not_public` items out of the public snapshot, and emits a search document for each published snippet. Refresh the checked-in `content/published/*.json` fixtures so they are outputs of the same compiler shape rather than hand-edited drift.</action>
  <verify>cd apps/api && go test ./internal/publish && pnpm check:published-index && test -f content/published/search-documents.json</verify>
  <acceptance_criteria>
    - `apps/api/internal/publish/artifacts.go` contains logic for `snippets.json`, `visibility.json`, and `search-documents.json`
    - `content/published/search-documents.json` exists and contains `"items"`
    - `apps/api/internal/publish/artifacts_test.go` asserts unpublished items stay out of `snippets.json`
    - `packages/snippet-schema/src/published-index.ts` still validates the snapshot/visibility contract after any needed extension
  </acceptance_criteria>
  <done>The publish phase now produces the exact public data surfaces that Discovery and future search indexing depend on.</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/api && go test ./internal/publish` passes
- [ ] `pnpm check:published-index` passes after artifact generation changes
- [ ] `content/published/snippets.json` and `content/published/visibility.json` remain parser-compatible
- [ ] `content/published/search-documents.json` is generated and reflects published-only content
</verification>

<success_criteria>
Publish readiness and artifact refresh are now coupled: a snippet can only become public by passing layered validation and regenerating the public artifacts Discovery/search consume.
</success_criteria>

<output>
After completion, create `.planning/phases/03-publish-pipeline/03-02-SUMMARY.md`
</output>
