---
phase: 02-discovery-experience
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/web/src/lib/discovery/types.ts
  - apps/web/src/lib/discovery/api.ts
  - apps/web/src/lib/discovery/query.ts
  - apps/web/src/lib/components/SnippetCard.svelte
  - apps/web/src/lib/components/FacetChips.svelte
  - apps/web/src/lib/components/CodeBlock.svelte
  - apps/web/src/lib/components/PromptBlock.svelte
  - apps/web/src/routes/+page.ts
  - apps/web/src/routes/+page.svelte
  - apps/web/src/routes/explore/+page.ts
  - apps/web/src/routes/explore/+page.svelte
  - apps/web/src/routes/snippets/[id]/+page.ts
  - apps/web/src/routes/snippets/[id]/+page.svelte
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DTL-01
  - DTL-02
  - DTL-03
  - DTL-04
must_haves:
  truths:
    - Users can browse a large-card public feed that exposes title, summary, tags, difficulty, platform, and cover-first media
    - Users can use a dedicated Explore page with URL-driven keyword and facet filtering plus zero-result recommendations
    - Users can open a detail page, see demo-first hero media, switch tabs, copy code/prompt blocks, and get an explicit not-public state for unpublished slugs
  artifacts:
    - path: apps/web/src/routes/+page.svelte
      provides: portfolio-style homepage feed
    - path: apps/web/src/routes/explore/+page.svelte
      provides: dedicated search and facet page
    - path: apps/web/src/routes/snippets/[id]/+page.svelte
      provides: demo-first detail page with copy blocks and not-public state
  key_links:
    - from: apps/web/src/lib/discovery/api.ts
      to: /api/v1/discovery/search
      why: Explore must reflect server-ranked results and facet filters, not client-only mock filtering
    - from: apps/web/src/routes/snippets/[id]/+page.ts
      to: /api/v1/discovery/snippets/{id}
      why: detail UI must consume the published-only API and propagate explicit not-public state
---

<objective>
Build the SvelteKit public discovery experience on top of the Phase 2 APIs.

Purpose: Deliver the actual user-facing browsing, search, and detail flows promised by the roadmap without inventing scope outside the locked Discovery boundary.
Output: Shared web discovery client/types/components plus home, explore, and snippet detail routes.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-discovery-experience/02-CONTEXT.md
@.planning/phases/02-discovery-experience/02-RESEARCH.md
@.planning/phases/02-discovery-experience/02-02-PLAN.md
@apps/web/src/routes/+page.ts
@apps/web/src/routes/+page.svelte
@apps/web/src/routes/health/+page.svelte

<interfaces>
Expected API contracts from `02-02`:
```ts
type FeedResponse = { items: PublishedSnippetCard[] };
type SearchResponse = {
  items: PublishedSnippetCard[];
  facets: {
    categories: Record<string, number>;
    difficulties: Record<string, number>;
    platforms: Record<string, number>;
    hasDemo: Record<string, number>;
    hasPrompt: Record<string, number>;
  };
  fallback: PublishedSnippetCard[];
};
type DetailResponse = PublishedSnippetRecord;
```

Locked UX constraints:
- D-01/D-03: feed uses large visual cover-first cards with static previews
- D-05/D-07: search lives on `/explore` and reacts immediately to query-param changes
- D-10/D-11/D-12: detail page uses tabs with per-block copy buttons for Code and Prompt, plus accessible License access
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared discovery web client and reusable UI blocks</name>
  <files>apps/web/src/lib/discovery/types.ts, apps/web/src/lib/discovery/api.ts, apps/web/src/lib/discovery/query.ts, apps/web/src/lib/components/SnippetCard.svelte, apps/web/src/lib/components/FacetChips.svelte, apps/web/src/lib/components/CodeBlock.svelte, apps/web/src/lib/components/PromptBlock.svelte</files>
  <read_first>.planning/phases/02-discovery-experience/02-CONTEXT.md, .planning/phases/02-discovery-experience/02-RESEARCH.md, .planning/phases/02-discovery-experience/02-02-PLAN.md, apps/web/src/routes/+page.svelte, apps/web/src/routes/+page.ts</read_first>
  <action>Create `apps/web/src/lib/discovery/types.ts` mirroring the API response shapes from Phase 2. Create `apps/web/src/lib/discovery/api.ts` with exact loader helpers `loadFeed(fetch)`, `loadSearch(fetch, params)`, and `loadSnippetDetail(fetch, id)` targeting `/api/v1/discovery/feed`, `/api/v1/discovery/search`, and `/api/v1/discovery/snippets/${id}`. Create `apps/web/src/lib/discovery/query.ts` with helpers that map the exact URL params `q`, `category`, `difficulty`, `platform`, `hasDemo`, and `hasPrompt`. Build four reusable components: `SnippetCard.svelte` for the large visual card with cover image, title, summary, tags, difficulty badge, and platform pills per D-01/D-02/D-03; `FacetChips.svelte` for lightweight top-chip filtering per D-06; `CodeBlock.svelte` with a one-click copy button labeled `Copy code`; and `PromptBlock.svelte` with a one-click copy button labeled `Copy prompt`. Do not add autoplay, hover-video playback, upload controls, admin links, or any Phase 3 actions.</action>
  <verify>pnpm --filter @swiftsnippet/web check</verify>
  <acceptance_criteria>
    - `apps/web/src/lib/discovery/api.ts` contains `/api/v1/discovery/feed`
    - `apps/web/src/lib/discovery/api.ts` contains `/api/v1/discovery/search`
    - `apps/web/src/lib/discovery/api.ts` contains `/api/v1/discovery/snippets/`
    - `apps/web/src/lib/components/SnippetCard.svelte` contains `difficulty`
    - `apps/web/src/lib/components/CodeBlock.svelte` contains `Copy code`
    - `apps/web/src/lib/components/PromptBlock.svelte` contains `Copy prompt`
  </acceptance_criteria>
  <done>The web app has reusable contracts and UI primitives aligned with the published discovery APIs and locked interaction constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Build the home, explore, and detail routes with explicit not-public handling</name>
  <files>apps/web/src/routes/+page.ts, apps/web/src/routes/+page.svelte, apps/web/src/routes/explore/+page.ts, apps/web/src/routes/explore/+page.svelte, apps/web/src/routes/snippets/[id]/+page.ts, apps/web/src/routes/snippets/[id]/+page.svelte</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-discovery-experience/02-CONTEXT.md, apps/web/src/lib/discovery/api.ts, apps/web/src/lib/components/SnippetCard.svelte, apps/web/src/routes/+page.ts, apps/web/src/routes/+page.svelte</read_first>
  <action>Replace the Phase 1 homepage stub with a public feed route that calls `loadFeed(fetch)`, renders a portfolio-like hero plus the published cards, and surfaces editorial order without introducing search controls heavier than a link to `/explore`. Create `/explore` as a dedicated page that reads URL params in `+page.ts`, calls `loadSearch(fetch, url.searchParams)`, shows a live keyword field plus lightweight facet chips for `category`, `difficulty`, `platform`, `hasDemo`, and `hasPrompt`, and updates the URL immediately on input/tap without an Apply button per D-05/D-07. When `items.length === 0`, render a clear no-results message plus `fallback` cards per D-08. Create `/snippets/[id]` so `+page.ts` fetches detail data, treats a `403 not_public` payload as `{ notPublic: true }`, and renders a dedicated “内容未公开” state instead of throwing a generic 404 per D-13. In `+page.svelte`, build a demo-first hero per D-09, tabbed content areas named exactly `Demo`, `Code`, `Prompt`, and `License` per D-10, show one copy button per code/prompt block per D-11, keep the License/Dependencies content in the License tab per D-12, hide the Prompt tab when `hasPrompt` is false, and fall back to the cover image when `demoUrl` is absent per D-15. Do not render placeholder tabs or “temporarily unavailable” copy for missing prompt/demo blocks per D-16.</action>
  <verify>pnpm --filter @swiftsnippet/web check && pnpm --filter @swiftsnippet/web build</verify>
  <acceptance_criteria>
    - `apps/web/src/routes/explore/+page.svelte` contains `No published snippets matched your current filters`
    - `apps/web/src/routes/explore/+page.svelte` contains `name="q"` or `bind:value`
    - `apps/web/src/routes/snippets/[id]/+page.svelte` contains `内容未公开`
    - `apps/web/src/routes/snippets/[id]/+page.svelte` contains `Demo`
    - `apps/web/src/routes/snippets/[id]/+page.svelte` contains `Code`
    - `apps/web/src/routes/snippets/[id]/+page.svelte` contains `License`
  </acceptance_criteria>
  <done>The SvelteKit app delivers the public discovery flow end-to-end: feed, dedicated explore search, detail tabs, copy affordances, and explicit not-public state.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter @swiftsnippet/web check` passes
- [ ] `pnpm --filter @swiftsnippet/web build` passes
- [ ] Home route renders published cards only
- [ ] Explore reacts to URL query params and shows fallback recommendations on zero results
- [ ] Detail route renders tabs and handles `not_public` without a generic 404
</verification>

<success_criteria>
Users can browse, narrow, open, and copy from public SwiftSnippet entries through the real Phase 2 routes, while unpublished content stays unlisted and direct non-public slugs show an explicit blocked state.
</success_criteria>

<output>
After completion, create `.planning/phases/02-discovery-experience/02-03-SUMMARY.md`
</output>
