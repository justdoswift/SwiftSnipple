---
phase: 08-admin-studio-v1
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - apps/web/package.json
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/components.json
  - apps/web/src/routes/layout.css
  - apps/web/src/lib/utils.ts
  - apps/web/src/lib/components/ui/**
  - apps/web/src/lib/hooks/**
  - apps/web/src/lib/components/studio/StudioSidebar.svelte
  - apps/web/src/routes/+layout.svelte
  - apps/web/src/routes/studio/+layout.svelte
  - apps/web/src/routes/studio/+page.svelte
  - apps/web/src/routes/studio/login/+page.svelte
  - apps/web/src/routes/studio/snippets/+page.svelte
  - apps/web/src/routes/studio/snippets/new/+page.svelte
  - apps/web/src/routes/studio/snippets/[id]/+page.svelte
  - apps/web/src/lib/discovery/discovery.smoke.test.ts
  - apps/web/e2e/studio.spec.ts
  - pnpm-lock.yaml
autonomous: true
requirements:
  - OPS-01
  - OPS-04
must_haves:
  truths:
    - Studio UI must move onto a reusable component system instead of continuing as one-off hand-rolled panels
    - Tailwind and shadcn-svelte must coexist with the current public discovery surface without forcing a full public-site rewrite
    - Playwright must remain the final arbiter for studio UI regressions
  artifacts:
    - path: apps/web/components.json
      provides: shadcn-svelte component registry and future add-component defaults
    - path: apps/web/src/routes/layout.css
      provides: Tailwind v4 global layer, tokens, and compatibility styles
    - path: apps/web/src/routes/studio/+layout.svelte
      provides: sidebar-backed studio shell on top of shadcn primitives
  key_links:
    - from: apps/web/src/routes/layout.css
      to: apps/web/src/routes/+layout.svelte
      why: global token and utility extraction must preserve the public shell while enabling shadcn styling
    - from: apps/web/src/lib/components/studio/StudioSidebar.svelte
      to: apps/web/src/routes/studio/+layout.svelte
      why: the studio shell depends on a stable reusable sidebar, not page-local navigation markup
---

> Historical note: this archived plan references the earlier `shadcn-svelte` path. Keep it as implementation history only; the active frontend stack is now different.

<objective>
Upgrade the admin studio UI foundation onto Tailwind v4 and shadcn-svelte, then refactor the main `/studio` routes into a more mature internal operations shell without rewriting the public discovery site.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-admin-studio-v1/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn-svelte foundation and migrate studio routes onto the new UI system</name>
  <files>apps/web/package.json, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/components.json, apps/web/src/routes/layout.css, apps/web/src/lib/utils.ts, apps/web/src/lib/components/ui/**, apps/web/src/lib/hooks/**, apps/web/src/lib/components/studio/StudioSidebar.svelte, apps/web/src/routes/+layout.svelte, apps/web/src/routes/studio/+layout.svelte, apps/web/src/routes/studio/+page.svelte, apps/web/src/routes/studio/login/+page.svelte, apps/web/src/routes/studio/snippets/+page.svelte, apps/web/src/routes/studio/snippets/new/+page.svelte, apps/web/src/routes/studio/snippets/[id]/+page.svelte, apps/web/src/lib/discovery/discovery.smoke.test.ts, apps/web/e2e/studio.spec.ts, pnpm-lock.yaml</files>
  <action>Use the official SvelteKit shadcn-svelte initialization flow to add Tailwind v4, components.json, UI primitives, and shared utilities. Extract the cross-page token layer into global CSS, then rebuild the studio shell, login, dashboard, list, create, and editor screens around sidebar/card/form/table/tabs primitives while keeping the public discovery pages visually compatible. Finish with Playwright-backed UI regression checks.</action>
  <verify>pnpm -C apps/web run check && pnpm -C apps/web run test && pnpm -C apps/web run test:e2e</verify>
  <acceptance_criteria>
    - `apps/web` has a working Tailwind v4 + shadcn-svelte foundation with `components.json`, UI primitives, and global tokens
    - `/studio` uses a sidebar-backed admin shell rather than the previous hand-rolled topbar-only layout
    - Login, dashboard, list, create, and editor pages all render on the new component system without breaking existing data flows
    - Existing discovery smoke tests and Playwright flows remain green after the UI foundation shift
  </acceptance_criteria>
  <done>Studio is now standing on a reusable component system, making future admin UX work incremental instead of one-off styling.</done>
</task>

</tasks>

<verification>
- [x] `pnpm -C apps/web run check`
- [x] `pnpm -C apps/web run test`
- [x] `pnpm -C apps/web run test:e2e`
</verification>

<success_criteria>
SwiftSnippet now has a reusable shadcn-svelte foundation in `apps/web` and a Studio UI that feels like a coherent internal product surface instead of a set of custom panels.
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-studio-v1/08-02-SUMMARY.md`
</output>
