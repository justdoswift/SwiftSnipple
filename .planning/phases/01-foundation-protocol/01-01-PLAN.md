---
phase: 01-foundation-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - pnpm-workspace.yaml
  - README.md
  - apps/web/package.json
  - apps/web/svelte.config.js
  - apps/web/vite.config.ts
  - apps/web/tsconfig.json
  - apps/web/src/app.d.ts
  - apps/web/src/app.html
  - apps/web/src/lib/constants.ts
  - apps/web/src/routes/+page.ts
  - apps/web/src/routes/+page.svelte
  - apps/web/src/routes/health/+page.ts
  - apps/web/src/routes/health/+page.svelte
  - apps/api/go.mod
  - apps/api/README.md
  - apps/api/cmd/api/main.go
  - apps/api/internal/config/config.go
  - apps/api/internal/httpapi/server.go
  - apps/api/internal/httpapi/server_test.go
  - apps/api/internal/version/version.go
  - infra/postgres/docker-compose.yml
  - infra/postgres/migrations/001_init.sql
  - scripts/db-migrate.sh
autonomous: true
requirements:
  - CPRT-04
  - OPS-02
user_setup: []
must_haves:
  truths:
    - Web stub can start and expose a minimal root page plus a health page
    - Go API stub can start and return successful responses from /health and /meta
    - Local PostgreSQL can be started with docker compose and initialized with a first migration
  artifacts:
    - path: apps/web/src/routes/+page.svelte
      provides: runnable Phase 1 web stub
    - path: apps/api/internal/httpapi/server.go
      provides: runnable Phase 1 API stub with minimal endpoints
    - path: infra/postgres/migrations/001_init.sql
      provides: baseline schema for snippet, version, asset, and publish state
  key_links:
    - from: package.json
      to: infra/postgres/docker-compose.yml
      why: root commands must start the local database contract used by later phases
    - from: apps/api/internal/config/config.go
      to: infra/postgres/migrations/001_init.sql
      why: API configuration must point at the database shape defined for Phase 1
---

> Historical note: this archived plan was written for an earlier `SvelteKit`-based frontend direction. Keep it as execution history only; do not use it as current stack guidance.

<objective>
Create the runnable repository skeleton and local database baseline for SwiftSnippet.

Purpose: Give later phases stable execution entrypoints instead of static directories.
Output: Monorepo root config, runnable SvelteKit stub, runnable Go API stub, dockerized PostgreSQL, first SQL migration, and database startup scripts.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-protocol/01-CONTEXT.md
@.planning/phases/01-foundation-protocol/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the repository skeleton and runnable stubs</name>
  <files>.gitignore, package.json, pnpm-workspace.yaml, README.md, apps/web/package.json, apps/web/svelte.config.js, apps/web/vite.config.ts, apps/web/tsconfig.json, apps/web/src/app.d.ts, apps/web/src/app.html, apps/web/src/lib/constants.ts, apps/web/src/routes/+page.ts, apps/web/src/routes/+page.svelte, apps/web/src/routes/health/+page.ts, apps/web/src/routes/health/+page.svelte, apps/api/go.mod, apps/api/README.md, apps/api/cmd/api/main.go, apps/api/internal/config/config.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go, apps/api/internal/version/version.go</files>
  <read_first>.planning/phases/01-foundation-protocol/01-CONTEXT.md, .planning/phases/01-foundation-protocol/01-RESEARCH.md, README.md</read_first>
  <action>Create the monorepo root files with pnpm workspace support, add the SvelteKit stub under `apps/web` with a root page and `/health` route, and add the Go API stub under `apps/api` with `/health`, `/meta`, and a reserved `/api/v1/snippets` route that returns `501 Not Implemented`. Use the exact Phase 1 messaging from the context files: the web page must describe repository shape, locked decisions, and health endpoints; the API metadata response must include `service`, `version`, `phase`, `environment`, `databaseUrl`, and `aiConfigured` keys. Keep the stubs runnable but intentionally non-business-functional.</action>
  <verify>pnpm --filter @swiftsnippet/web check && pnpm --filter @swiftsnippet/web build && pnpm build:api && cd apps/api && go test ./...</verify>
  <acceptance_criteria>
    - `apps/web/src/routes/+page.svelte` contains `Single-repo modular structure`
    - `apps/web/src/routes/health/+page.svelte` contains `Web health`
    - `apps/api/internal/httpapi/server.go` contains `"/health"` and `"/meta"` and `"/api/v1/snippets"`
    - `apps/api/internal/httpapi/server.go` contains `snippet APIs are not implemented in Phase 1`
    - `package.json` contains `dev:web` and `build:api`
  </acceptance_criteria>
  <done>Web and API stubs build successfully and expose the Phase 1 health interfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Establish the local PostgreSQL baseline and migration flow</name>
  <files>infra/postgres/docker-compose.yml, infra/postgres/migrations/001_init.sql, scripts/db-migrate.sh, apps/api/internal/config/config.go, README.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-foundation-protocol/01-CONTEXT.md, infra/postgres/docker-compose.yml, infra/postgres/migrations/001_init.sql</read_first>
  <action>Add a Docker Compose file for a local PostgreSQL 16 container using database `swiftsnippet`, user `swiftsnippet`, password `swiftsnippet`, and port `5432`. Add `001_init.sql` creating the exact tables `snippet`, `snippet_version`, `publish_state`, and `snippet_asset`, including a `publish_state.state` check constraint limited to `draft`, `review`, and `published`. Add `scripts/db-migrate.sh` that applies every SQL file in `infra/postgres/migrations` via `psql` using `DATABASE_URL`, defaulting to `postgres://swiftsnippet:swiftsnippet@127.0.0.1:5432/swiftsnippet?sslmode=disable`. Ensure API config defaults to the same connection string and README documents `pnpm db:up` and `pnpm db:migrate`.</action>
  <verify>grep -n "create table if not exists snippet" infra/postgres/migrations/001_init.sql && grep -n "state in ('draft', 'review', 'published')" infra/postgres/migrations/001_init.sql && grep -n "postgres://swiftsnippet:swiftsnippet@127.0.0.1:5432/swiftsnippet?sslmode=disable" scripts/db-migrate.sh apps/api/internal/config/config.go README.md</verify>
  <acceptance_criteria>
    - `infra/postgres/docker-compose.yml` contains `postgres:16`
    - `infra/postgres/migrations/001_init.sql` contains `create table if not exists snippet_version`
    - `infra/postgres/migrations/001_init.sql` contains `create table if not exists publish_state`
    - `scripts/db-migrate.sh` contains `psql "$DB_URL" -v ON_ERROR_STOP=1 -f "$file"`
    - `README.md` contains `pnpm db:up` and `pnpm db:migrate`
  </acceptance_criteria>
  <done>The repository has a real local PostgreSQL baseline and a reproducible migration entrypoint.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @swiftsnippet/web check` passes
- [ ] `pnpm --filter @swiftsnippet/web build` passes
- [ ] `pnpm build:api` passes
- [ ] `cd apps/api && go test ./...` passes
- [ ] Root and API docs reference the local PostgreSQL workflow
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has runnable Web and API stubs plus a real local PostgreSQL baseline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-protocol/01-01-SUMMARY.md`
</output>
