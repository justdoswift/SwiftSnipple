---
phase: 03-publish-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/internal/config/config.go
  - apps/api/internal/httpapi/server.go
  - apps/api/internal/httpapi/server_test.go
  - apps/api/internal/publish/repository.go
  - apps/api/internal/publish/repository_test.go
  - apps/api/internal/publish/service.go
  - apps/api/internal/publish/service_test.go
  - apps/api/internal/storage/presign.go
  - apps/api/internal/storage/presign_test.go
  - apps/api/internal/ratelimit/limiter.go
  - apps/api/internal/ratelimit/limiter_test.go
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - OPS-01
must_haves:
  truths:
    - Maintainers can request a constrained upload URL without receiving direct cloud credentials
    - The system has explicit `draft -> review -> published` transition primitives backed by existing publish-state storage
    - Resource-sensitive write endpoints have basic rate limiting before Phase 3 exposes publish actions
  artifacts:
    - path: apps/api/internal/storage/presign.go
      provides: signed-upload request validation and presign response generation boundary
    - path: apps/api/internal/publish/service.go
      provides: publish state transition operations over the existing PostgreSQL-backed model
    - path: apps/api/internal/ratelimit/limiter.go
      provides: reusable endpoint-level token-bucket limiter for write APIs
  key_links:
    - from: apps/api/internal/httpapi/server.go
      to: apps/api/internal/storage/presign.go
      why: the upload-url endpoint must delegate signing to a dedicated adapter instead of inlining storage logic
    - from: apps/api/internal/publish/service.go
      to: infra/postgres/migrations/001_init.sql
      why: publish transitions must map directly onto the existing `publish_state`, `snippet_version`, and `snippet_asset` schema
    - from: apps/api/internal/httpapi/server.go
      to: apps/api/internal/ratelimit/limiter.go
      why: upload and publish write routes need operational safety at the HTTP boundary
---

<objective>
Build the write-side publish domain foundation for Phase 3.

Purpose: Establish the minimum backend primitives required for signed uploads, explicit publish-state transitions, and write-path rate limiting before artifact generation is added.
Output: A publish domain service, storage presign adapter, rate-limit middleware, and route-level tests proving upload-url issuance and state transitions are protected and explicit.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-publish-pipeline/03-CONTEXT.md
@.planning/phases/03-publish-pipeline/03-RESEARCH.md
@infra/postgres/migrations/001_init.sql
@apps/api/internal/config/config.go
@apps/api/internal/httpapi/server.go
@packages/snippet-schema/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the signed-upload adapter and request validation boundary</name>
  <files>apps/api/internal/config/config.go, apps/api/internal/storage/presign.go, apps/api/internal/storage/presign_test.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go</files>
  <read_first>.planning/phases/03-publish-pipeline/03-CONTEXT.md, .planning/phases/03-publish-pipeline/03-RESEARCH.md, apps/api/internal/config/config.go, apps/api/internal/httpapi/server.go, packages/snippet-schema/src/types.ts</read_first>
  <action>Create `apps/api/internal/storage/presign.go` with a small adapter that accepts only the exact Phase 3 upload request concepts: `snippetID`, `version`, `assetKind`, `contentType`, `contentLength`, and optional content hash. Restrict `assetKind` to the Discovery-relevant media classes `cover` and `demo`; derive the object key as `snippet_id/version/asset_type` rather than accepting arbitrary client-provided paths. Extend `apps/api/internal/config/config.go` with the exact environment-style config keys needed for this adapter, following the repo’s `envOrDefault` pattern. Add `POST /api/v1/media/upload-url` to `apps/api/internal/httpapi/server.go`; it must parse JSON, validate required fields, delegate to the presign adapter, and return a structured JSON response containing the object key, method, upload URL, and any required headers, without exposing provider credentials. Add route tests in `apps/api/internal/httpapi/server_test.go` and unit tests in `apps/api/internal/storage/presign_test.go` that prove invalid asset kinds, empty snippet ids, and malformed requests are rejected.</action>
  <verify>cd apps/api && go test ./internal/storage ./internal/httpapi</verify>
  <acceptance_criteria>
    - `apps/api/internal/httpapi/server.go` contains `"/api/v1/media/upload-url"`
    - `apps/api/internal/storage/presign.go` contains `assetKind`
    - `apps/api/internal/storage/presign.go` contains `cover` and `demo`
    - `apps/api/internal/config/config.go` contains at least one new storage-related env key
    - `apps/api/internal/httpapi/server_test.go` contains a test for `/api/v1/media/upload-url`
  </acceptance_criteria>
  <done>The API can issue constrained signed upload instructions for publish media without turning into a generic storage proxy.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit publish-state transitions and endpoint-focused rate limiting</name>
  <files>apps/api/internal/publish/repository.go, apps/api/internal/publish/repository_test.go, apps/api/internal/publish/service.go, apps/api/internal/publish/service_test.go, apps/api/internal/ratelimit/limiter.go, apps/api/internal/ratelimit/limiter_test.go, apps/api/internal/httpapi/server.go, apps/api/internal/httpapi/server_test.go</files>
  <read_first>.planning/phases/03-publish-pipeline/03-CONTEXT.md, .planning/phases/03-publish-pipeline/03-RESEARCH.md, infra/postgres/migrations/001_init.sql, apps/api/internal/httpapi/server.go</read_first>
  <action>Create a `publish` package under `apps/api/internal/publish/` that reads and writes the existing `publish_state`, `snippet_version`, and `snippet_asset` tables as the minimum state authority. Implement explicit service methods for `MoveToReview` and `PublishVersion` that enforce only the allowed `draft -> review -> published` transitions and return structured domain errors for illegal state changes or missing versions. Add `apps/api/internal/ratelimit/limiter.go` as a reusable token-bucket style middleware package with tests that prove repeated requests are rejected after the configured burst. Wire the limiter into `apps/api/internal/httpapi/server.go` so it protects the upload-url handler and the new publish-state handlers, rather than public read routes. Add HTTP endpoints for `POST /api/v1/publish/snippets/{id}/review` and `POST /api/v1/publish/snippets/{id}/publish`; the publish endpoint must require a version identifier and must delegate to the publish service instead of mutating state inline inside the handler.</action>
  <verify>cd apps/api && go test ./internal/publish ./internal/ratelimit ./internal/httpapi</verify>
  <acceptance_criteria>
    - `apps/api/internal/publish/service.go` contains `MoveToReview`
    - `apps/api/internal/publish/service.go` contains `PublishVersion`
    - `apps/api/internal/httpapi/server.go` contains `"/api/v1/publish/snippets/"`
    - `apps/api/internal/ratelimit/limiter.go` contains a limiter type or middleware constructor
    - `apps/api/internal/httpapi/server_test.go` contains at least one rate-limit or transition error assertion
  </acceptance_criteria>
  <done>Phase 3 has explicit, testable state transitions and a minimal safety net around the most abuse-prone write endpoints.</done>
</task>

</tasks>

<verification>
- [ ] `cd apps/api && go test ./internal/storage ./internal/publish ./internal/ratelimit ./internal/httpapi` passes
- [ ] Upload requests do not accept arbitrary asset kinds or arbitrary object keys
- [ ] Publish-state transitions reject illegal moves and require explicit version context for publish
- [ ] Write-path rate limiting is attached to upload/publish endpoints rather than feed/search/detail routes
</verification>

<success_criteria>
The repository exposes the foundational write-side primitives for Phase 3: signed uploads, explicit publish transitions, and rate-limited write endpoints that later plans can build artifact refresh on top of.
</success_criteria>

<output>
After completion, create `.planning/phases/03-publish-pipeline/03-01-SUMMARY.md`
</output>
