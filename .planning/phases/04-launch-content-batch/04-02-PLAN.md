---
phase: 04-launch-content-batch
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01-PLAN.md
files_modified:
  - content/snippets/universal-card-container/snippet.yaml
  - content/snippets/masonry-grid/snippet.yaml
  - content/snippets/skeleton-shimmer/snippet.yaml
  - content/snippets/tag-chips-wrap/snippet.yaml
  - content/snippets/search-page-skeleton/snippet.yaml
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
must_haves:
  truths:
    - The public feed becomes visually diverse through showcase-heavy layout and loading snippets instead of repeating one card pattern
    - Only `masonry-grid` and `skeleton-shimmer` consume demo budget in this showcase batch; the other three ship as cover-only
    - Every showcase snippet keeps a single clear copy-first reuse path for SwiftUI developers
  artifacts:
    - path: content/snippets/masonry-grid/snippet.yaml
      provides: the second demo-backed launch anchor with a grid-centric layout story
    - path: content/snippets/skeleton-shimmer/snippet.yaml
      provides: the loading-state showcase entry with motion value obvious enough to justify demo media
    - path: content/snippets/search-page-skeleton/snippet.yaml
      provides: a cover-only loading/search composition that broadens feed coverage without requiring video
  key_links:
    - from: content/snippets/masonry-grid/snippet.yaml
      to: content/published/snippets.json
      why: the showcase batch must introduce a visually distinct high-rank card into the public feed
    - from: content/snippets/skeleton-shimmer/snippet.yaml
      to: content/published/search-documents.json
      why: loading-state snippets still need searchable metadata and tags despite being highly visual
---

<objective>
Create the showcase-heavy half of the launch batch.

Purpose: Fill the public feed with enough visual variety that SwiftSnippet reads like a useful snippet library, not only a protocol demo repository.
Output: Five new launch snippets covering reusable layout containers, grid composition, loading states, and tag wrapping, with demo media reserved for the two highest-motion entries.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-launch-content-batch/04-CONTEXT.md
@idea.md
@content/snippets/basic-card-feed/snippet.yaml
@content/snippets/stacked-hero-card/snippet.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the three highest-visibility showcase snippets</name>
  <files>content/snippets/universal-card-container/snippet.yaml, content/snippets/masonry-grid/snippet.yaml, content/snippets/skeleton-shimmer/snippet.yaml</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, idea.md, content/snippets/basic-card-feed/snippet.yaml, content/snippets/stacked-hero-card/snippet.yaml</read_first>
  <action>Create `content/snippets/universal-card-container`, `content/snippets/masonry-grid`, and `content/snippets/skeleton-shimmer` using the existing snippet directory protocol. Set exact ids to those three slugs. Keep them SwiftUI-first with `Code/SwiftUI`, `Code/Vibe`, `LICENSES`, `Media`, and `Tests` paths wired in `snippet.yaml`. Make `universal-card-container` cover-only. Make `masonry-grid` and `skeleton-shimmer` demo-backed by including both `assets.cover` and `assets.demo`. Use concise English titles and summaries oriented around immediate reuse, not internal implementation details. Keep tags and facets specific enough for Explore queries such as `grid`, `skeleton`, `loading`, `layout`, and `swiftui`.</action>
  <verify>./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/universal-card-container && ./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/masonry-grid && ./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/skeleton-shimmer</verify>
  <acceptance_criteria>
    - `content/snippets/universal-card-container/snippet.yaml` contains `id: universal-card-container`
    - `content/snippets/masonry-grid/snippet.yaml` contains `id: masonry-grid`
    - `content/snippets/masonry-grid/snippet.yaml` contains `demo: Media/demo.mp4`
    - `content/snippets/skeleton-shimmer/snippet.yaml` contains `id: skeleton-shimmer`
    - `content/snippets/skeleton-shimmer/snippet.yaml` contains `demo: Media/demo.mp4`
  </acceptance_criteria>
  <done>The feed now has three new showcase anchors, including two demo-backed snippets that can carry the visual side of the launch.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two cover-only showcase support snippets</name>
  <files>content/snippets/tag-chips-wrap/snippet.yaml, content/snippets/search-page-skeleton/snippet.yaml</files>
  <read_first>.planning/phases/04-launch-content-batch/04-CONTEXT.md, idea.md, content/snippets/masonry-grid/snippet.yaml, content/snippets/skeleton-shimmer/snippet.yaml</read_first>
  <action>Create `content/snippets/tag-chips-wrap` and `content/snippets/search-page-skeleton` as cover-only launch snippets. Keep `tag-chips-wrap` focused on wrapping filter chips / tags for dense browse surfaces. Keep `search-page-skeleton` focused on search-result loading states that visually connect to the discovery experience shipped in Phase 2. Both snippets must include copyable SwiftUI code, prompt assets under `Code/Vibe`, launch-ready license fields, and media cover paths in `snippet.yaml`, but must not claim a demo asset.</action>
  <verify>./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/tag-chips-wrap && ./packages/snippet-schema/node_modules/.bin/tsx packages/snippet-schema/src/cli.ts snippet content/snippets/search-page-skeleton</verify>
  <acceptance_criteria>
    - `content/snippets/tag-chips-wrap/snippet.yaml` contains `id: tag-chips-wrap`
    - `content/snippets/tag-chips-wrap/snippet.yaml` contains `cover: Media/cover.png`
    - `content/snippets/search-page-skeleton/snippet.yaml` contains `id: search-page-skeleton`
    - `content/snippets/search-page-skeleton/snippet.yaml` contains `cover: Media/cover.png`
    - `content/snippets/search-page-skeleton/snippet.yaml` does not contain `demo: Media/demo.mp4`
  </acceptance_criteria>
  <done>The showcase batch is broad enough to change how the homepage feels without turning video production into the bottleneck.</done>
</task>

</tasks>

<verification>
- [ ] All five showcase snippets pass schema validation
- [ ] Exactly two showcase snippets (`masonry-grid`, `skeleton-shimmer`) declare `assets.demo`
- [ ] The other three showcase snippets remain cover-only
- [ ] Every showcase snippet includes both `Code/SwiftUI` and `Code/Vibe` roots in `snippet.yaml`
</verification>

<success_criteria>
The public feed can now show several clearly different, launch-worthy visual patterns while still keeping the content batch fast enough to finish.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-content-batch/04-02-SUMMARY.md`
</output>
